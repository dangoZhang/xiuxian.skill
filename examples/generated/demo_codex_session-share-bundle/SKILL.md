---
name: vibecoding-profile-f11a0caa
description: 这是从码奸的真实记录蒸馏出的结果 skill，供 vibecoding.skill 读取或直接调用，用来复用这套协作方式。
---

# 码奸.skill

## 这份二级 skill 蒸馏了什么

- 任务定义层：目标 framing、上下文供给、约束治理、沟通压缩度
- 执行控制层：执行默认、任务拆解、工具编排、上下文承接、迭代修正、失败恢复
- 结果闭环层：验证闭环、产物落地、交接与记忆、抽象复用
- 杠杆放大量：自主推进深度、并行与工作流化

## 各层得分

- 任务定义层：`3.06/4` · 已观测 `4` / `4` 维
- 执行控制层：`0.70/4` · 已观测 `3` / `6` 维
- 结果闭环层：`1.79/4` · 已观测 `3` / `4` 维
- 杠杆放大量：`0.00/4` · 已观测 `0` / `2` 维

## 当前蒸馏结果

### 任务定义层

#### 目标 framing
- 评分：`4/4` · 置信度：`0.61` · 证据：`3` 条
- 判断：起手偏好先说清目标、边界和验收，再让 agent 接手执行。
- 锚点：`0` 只有模糊需求，目标和验收都不稳。 `2` 目标和部分约束已出现，但起手动作或验收还不完整。 `4` 目标、边界、验收和起手动作都清楚，agent 可直接接手。
- 证据：我们做一个新的 skill。目标是解析 codex 日志并生成一张修仙修为卡。约束：纯本地、独立 git、README 要像爆款 skill 项目。输出物：CLI、SKILL.md、README、示例。
- 证据：我先确认日志格式和仓库结构，再搭一个独立目录，把解析、打分和单卡输出接起来。

#### 上下文供给
- 评分：`4/4` · 置信度：`0.70` · 证据：`4` 条
- 判断：偏好把路径、文件、背景和历史直接交给 agent，减少来回确认。
- 锚点：`0` 几乎不给上下文，agent 需要边做边猜。 `2` 能给部分路径、文件或背景，但仍有缺口。 `4` 路径、文件、背景和历史一次给够，来回确认很少。
- 证据：我们做一个新的 skill。目标是解析 codex 日志并生成一张修仙修为卡。约束：纯本地、独立 git、README 要像爆款 skill 项目。输出物：CLI、SKILL.md、README、示例。
- 证据：我先确认日志格式和仓库结构，再搭一个独立目录，把解析、打分和单卡输出接起来。

#### 约束治理
- 评分：`1/4` · 置信度：`0.39` · 证据：`1` 条
- 判断：已有初步信号，说明会主动把必须项、禁忌和优先级讲清，避免执行时跑偏。
- 锚点：`0` 限制条件很少，执行边界不清。 `2` 有部分必须项或禁忌，但优先级不够清楚。 `4` 必须项、禁忌、优先级和兼容要求都可直接执行。
- 证据：我们做一个新的 skill。目标是解析 codex 日志并生成一张修仙修为卡。约束：纯本地、独立 git、README 要像爆款 skill 项目。输出物：CLI、SKILL.md、README、示例。

#### 沟通压缩度
- 评分：`3/4` · 置信度：`0.52` · 证据：`2` 条
- 判断：真实消息本身就偏短句、高密度、结论优先，不主要靠口头强调简洁。
- 锚点：`0` 表达散、长、低密度，关键信息埋得深。 `2` 会要求简洁，但并不稳定。 `4` 长期偏好短句、高密度、结论优先的表达。
- 证据：我们做一个新的 skill。目标是解析 codex 日志并生成一张修仙修为卡。约束：纯本地、独立 git、README 要像爆款 skill 项目。输出物：CLI、SKILL.md、README、示例。
- 证据：进一步加上 Claude Code / OpenCode 适配，README 里写清默认目录和下一周期突破机制。

### 执行控制层

#### 执行默认
- 评分：`1/4` · 置信度：`0.39` · 证据：`1` 条
- 判断：已有初步信号，说明更偏好直接动手、直接出结果，解释不要挡在执行前面。
- 锚点：`0` 长期停在解释或讨论里。 `2` 愿意直接做，但仍经常先讲一大段方案。 `4` 默认先执行，再回报，说明不挡路。
- 证据：我们做一个新的 skill。目标是解析 codex 日志并生成一张修仙修为卡。约束：纯本地、独立 git、README 要像爆款 skill 项目。输出物：CLI、SKILL.md、README、示例。

#### 任务拆解
- 评分：`0/4` · 置信度：`0.25` · 证据：`0` 条
- 判断：当前记录里，任务拆解能力信号还不够多。
- 锚点：`0` 几乎不拆任务，推进路径模糊。 `2` 能列步骤，但对执行收敛帮助一般。 `4` 会稳定拆步、排优先级，并持续收束下一步。

#### 工具编排
- 评分：`2/4` · 置信度：`0.55` · 证据：`2` 条
- 判断：倾向让 agent 读文件、跑命令、查日志、落脚本，而不是停在口头判断。
- 锚点：`0` 几乎不用工具，主要靠口头判断。 `2` 会用少量工具验证，但覆盖面有限。 `4` 文件、命令、日志、脚本和连接器会联动使用。
- 证据：我们做一个新的 skill。目标是解析 codex 日志并生成一张修仙修为卡。约束：纯本地、独立 git、README 要像爆款 skill 项目。输出物：CLI、SKILL.md、README、示例。
- 证据：我先确认日志格式和仓库结构，再搭一个独立目录，把解析、打分和单卡输出接起来。

#### 上下文承接
- 评分：`0/4` · 置信度：`0.25` · 证据：`0` 条
- 判断：当前记录里，上下文承接信号还不够稳定。
- 锚点：`0` 回合一长就容易丢主线。 `2` 大体能续住，但长链路仍会掉线。 `4` 长回合仍能稳定沿用上下文，不容易跑偏。

#### 迭代修正
- 评分：`1/4` · 置信度：`0.41` · 证据：`1` 条
- 判断：已有初步信号，说明倾向短回合迭代，看到偏差就补一条关键修正继续推进。
- 锚点：`0` 偏了以后修得慢，甚至不修。 `2` 会修正，但常常改得太大或太慢。 `4` 能快速定位关键偏差，最小修正后继续推进。
- 证据：我会先把 Codex 跑通，再为 Claude Code / OpenCode 留出入口。 最后补单卡示例输出，并做一次本地验证。

#### 失败恢复
- 评分：`0/4` · 置信度：`0.25` · 证据：`0` 条
- 判断：当前记录里，失败恢复信号还不够明确。
- 锚点：`0` 一遇阻力就停住或原地打转。 `2` 有 fallback 意识，但恢复不稳定。 `4` 能在失败后快速缩范围、换路并保持推进。

### 结果闭环层

#### 验证闭环
- 评分：`4/4` · 置信度：`0.61` · 证据：`3` 条
- 判断：默认要可验证结果，最好顺带交代验证方式和未验证项。
- 锚点：`0` 几乎不验证，停在“看起来差不多”。 `2` 有部分验证，但回报不完整。 `4` 会交代怎么验、证据在哪、还有什么没验。
- 证据：我们做一个新的 skill。目标是解析 codex 日志并生成一张修仙修为卡。约束：纯本地、独立 git、README 要像爆款 skill 项目。输出物：CLI、SKILL.md、README、示例。
- 证据：我先确认日志格式和仓库结构，再搭一个独立目录，把解析、打分和单卡输出接起来。

#### 产物落地
- 评分：`2/4` · 置信度：`0.48` · 证据：`2` 条
- 判断：偏好把结果落成 README、脚本、JSON、卡片或导出包这类明确产物。
- 锚点：`0` 主要停在口头结论，没有稳定产物。 `2` 能落出单个产物，但交付链还不完整。 `4` 会稳定产出可交付、可复用的一组结果文件。
- 证据：我们做一个新的 skill。目标是解析 codex 日志并生成一张修仙修为卡。约束：纯本地、独立 git、README 要像爆款 skill 项目。输出物：CLI、SKILL.md、README、示例。
- 证据：进一步加上 Claude Code / OpenCode 适配，README 里写清默认目录和下一周期突破机制。

#### 交接与记忆
- 评分：`0/4` · 置信度：`0.25` · 证据：`0` 条
- 判断：当前记录里，交接与记忆信号还不够稳定。
- 锚点：`0` 每轮几乎重来，没有交接意识。 `2` 会引用上次结果，但机制还不稳。 `4` 有明确的跨轮交接、导出包或结果 skill 接管信号。

#### 抽象复用
- 评分：`1/4` · 置信度：`0.39` · 证据：`1` 条
- 判断：已有初步信号，说明会把一次成功沉成 skill、模板、流程、规则或模块，方便继续复用。
- 锚点：`0` 一次一做，很少沉淀抽象。 `2` 会抽局部套路，但复用层还浅。 `4` 能主动沉成 skill、模板、流程和模块。
- 证据：我们做一个新的 skill。目标是解析 codex 日志并生成一张修仙修为卡。约束：纯本地、独立 git、README 要像爆款 skill 项目。输出物：CLI、SKILL.md、README、示例。

### 杠杆放大量

#### 自主推进深度
- 评分：`0/4` · 置信度：`0.25` · 证据：`0` 条
- 判断：当前记录里，自主推进深度还缺少足够证据。
- 锚点：`0` 基本每一步都要追问。 `2` 能推进 2 到 3 步，但复杂度一高就掉。 `4` 少追问下仍能稳定推进多步并主动补关键动作。

#### 并行与工作流化
- 评分：`0/4` · 置信度：`0.25` · 证据：`0` 条
- 判断：当前记录里，并行和工作流化信号还不够多。
- 锚点：`0` 仍以单线程聊天推进为主。 `2` 已有少量并行、自动化或工作流信号。 `4` 多 agent、多工具、工作流和传播链路都已进入主流程。

## 二级 skill 默认行为

- 起手偏好先说清目标、边界和验收，再让 agent 接手执行。
- 偏好把路径、文件、背景和历史直接交给 agent，减少来回确认。
- 已有初步信号，说明更偏好直接动手、直接出结果，解释不要挡在执行前面。
- 默认要可验证结果，最好顺带交代验证方式和未验证项。
- 倾向让 agent 读文件、跑命令、查日志、落脚本，而不是停在口头判断。
- 已有初步信号，说明倾向短回合迭代，看到偏差就补一条关键修正继续推进。

## Guardrails

- 已有初步信号，说明会主动把必须项、禁忌和优先级讲清，避免执行时跑偏。
- 真实消息本身就偏短句、高密度、结论优先，不主要靠口头强调简洁。
- 如果事实不够，先补文件、日志或命令结果，不要硬猜。

## Good Prompts

- 按 码奸 这套 vibecoding 习惯和我一起推进这个任务。
- 先收束目标、边界、验收，再直接开始做。
- 先读相关文件并给我可验证结果，不要只讲方案。
- 如果发现偏差，只补一条最关键修正，然后继续推进。

## Called By vibecoding.skill

- 当 `vibecoding.skill` 收到这份导出包时，应先读取 `PROFILE.md`、`REPORT.md`、`DISTILLED_SKILL.json`，再调用 `vibecoding-profile-f11a0caa`。
- 如果当前仓库指令和这份结果 skill 冲突，以更高优先级指令为准。
